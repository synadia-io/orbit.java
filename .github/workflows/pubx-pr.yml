name: Publish Extensions Pull Request

on:
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - 'js-publish-extensions/**'

jobs:
  build:
    uses: synadia-io/workflows/.github/workflows/java-standard-pr.yml@main
    with:
      project-dir: js-publish-extensions
    secrets: inherit
