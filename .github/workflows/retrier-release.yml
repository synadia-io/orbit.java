name: Retrier Release

on:
  push:
    tags: [ 'retrier/*' ]

jobs:
  build:
    uses: synadia-io/workflows/.github/workflows/java-standard-release.yml@main
    with:
      project-dir: retrier
    secrets: inherit
