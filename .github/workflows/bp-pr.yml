name: Batch Publish Pull Request

on:
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - 'batch-publish/**'

jobs:
  build:
    uses: synadia-io/workflows/.github/workflows/java-standard-pr.yml@main
    with:
      project-dir: batch-publish
    secrets: inherit
